if place_meeting(x-4,y,obj_player) && (obj_player.key_right)
{
	x += 4;
	
}

if place_meeting(x+4,y,obj_player) && (obj_player.key_left)
{
	x -= 4;
	
}

if place_meeting(x-4,y,obj_player_2) && (obj_player_2.key_right)
{
	x += 4;
	
}

if place_meeting(x+4,y,obj_player_2) && (obj_player_2.key_left)
{
	x -= 4;
	
}

